# Use the classic node_modules layout instead of Yarn Berry's Plug'n'Play.
# PnP breaks several of the Node CLIs we depend on (Prism, some Spectral
# plugins, Redocly's worker model). `node-modules` keeps Berry's lockfile
# and workspace features while restoring Classic's runtime compatibility.
nodeLinker: node-modules
