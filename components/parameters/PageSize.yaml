name: pageSize
in: query
required: false
description: |
  Page size. Subject to a server-side result-window cap: `page * pageSize`
  must not exceed the configured limit, currently 5,000 items (a request that
  exceeds it returns `400`).
schema:
  type: integer
  minimum: 1
  maximum: 5000
  default: 50
