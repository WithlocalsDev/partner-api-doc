name: pageSize
in: query
required: false
description: |
  Page size. Subject to a server-side result-window cap (`page * pageSize`
  must stay under the configured limit, currently 10,000 items).
schema:
  type: integer
  minimum: 1
  maximum: 200
  default: 50
