get:
  tags: [Supplier]
  operationId: getSupplier
  summary: Supplier
  description: |
    Returns the supplier's self-description.

    v1 surfaces:
      - `supplierName` — identifies the supplier.
      - `endpoint` — canonical base URL of this API.
      - `supportsIdempotencyKey` — currently `false`

  x-maps-to: "static config + PartnerContext (no existing endpoint today)"
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema: { $ref: "../components/schemas/Supplier.yaml" }
    "401": { $ref: "../components/responses/Unauthorized.yaml" }
