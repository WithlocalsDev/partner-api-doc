{
  "name": "withlocals-partner-api-spec",
  "version": "0.1.0-draft",
  "private": true,
  "description": "OpenAPI 3.1 spec for the Withlocals partner API.",
  "packageManager": "yarn@4.13.0",
  "scripts": {
    "bundle": "redocly bundle openapi.yaml -o dist/api-reference/openapi.yaml",
    "docs": "node -e \"const f=require('fs');f.mkdirSync('dist/api-reference',{recursive:true});f.copyFileSync('index.html','dist/api-reference/index.html');f.copyFileSync('redirect.html','dist/index.html');console.log('copied index.html -> dist/api-reference/index.html');console.log('copied redirect.html -> dist/index.html');\"",
    "copy-assets": "node -e \"const f=require('fs');if(f.existsSync('assets')){f.cpSync('assets','dist/api-reference/assets',{recursive:true});console.log('copied assets/ -> dist/api-reference/assets/');}\"",
    "mock": "prism mock openapi.yaml",
    "lint:spectral": "spectral lint dist/api-reference/openapi.yaml --ruleset .spectral.yaml",
    "lint": "yarn bundle && yarn lint:spectral",
    "build": "rimraf dist && yarn bundle && yarn docs && yarn copy-assets",
    "preview": "yarn build && serve dist -l 3001"
  },
  "devDependencies": {
    "@redocly/cli": "^1.25.0",
    "@stoplight/prism-cli": "^5.10.0",
    "@stoplight/spectral-cli": "^6.13.0",
    "rimraf": "^5.0.5",
    "serve": "^14.2.6"
  }
}
